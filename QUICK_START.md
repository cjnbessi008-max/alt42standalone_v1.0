# 빠른 시작 가이드

## 1분 만에 실행하기

### 1단계: 의존성 설치

```bash
cd frontend
npm install
```

### 2단계: 개발 서버 실행

```bash
npm run dev
```

### 3단계: 브라우저에서 확인

브라우저가 자동으로 열리며 `http://localhost:3000` 에서 앱을 확인할 수 있습니다.

## 화면 구성

### 메인 화면

1. **상단 앱바**: 시스템 제목 및 브랜딩
2. **소개 섹션**: 시스템 설명
3. **문제 선택 버튼**: 분수/기하/방정식 문제 전환
4. **문제 표시 영역**:
   - 문제 제목 및 설명
   - 색상별로 분류된 조건들
   - 선택된 조건 상세 정보
   - 색상 범례
5. **시스템 특징 섹션**: 주요 기능 설명

## 주요 기능 테스트

### 조건 색상 확인하기

각 조건은 다음 색상으로 표시됩니다:

- **파란색**: 주어진 값 (예: "첫 번째 분수: 3/4")
- **초록색**: 제약조건 (예: "분모는 0이 아니어야 함")
- **주황색**: 범위조건 (예: "모든 변의 길이 > 0")
- **빨간색**: 중요조건 (예: "답은 기약분수로 표현")
- **보라색**: 조건부로직 (예: "분모가 다르면 통분 필요")

### 인터랙션 테스트하기

1. **마우스 호버**: 조건 위에 마우스를 올리면 설명 툴팁이 표시됩니다
2. **클릭**: 조건을 클릭하면 하단에 상세 설명이 표시됩니다
3. **문제 전환**: 상단 버튼으로 다른 문제 유형을 볼 수 있습니다

## 문제 유형별 예시

### 분수 문제
- 분수의 덧셈 문제
- 6개의 조건 (값 2개, 제약 2개, 중요 1개, 조건부 1개)

### 기하 문제
- 삼각형 넓이 구하기
- 5개의 조건 (값 2개, 범위 1개, 제약 1개, 중요 1개)

### 방정식 문제
- 일차방정식 풀이
- 6개의 조건 (값 1개, 제약 1개, 조건부 2개, 중요 1개, 범위 1개)

## 커스터마이징

### 색상 변경하기

`frontend/src/config/colorScheme.ts` 파일을 수정하세요.

### 새 문제 추가하기

`frontend/src/config/sampleProblems.ts` 파일에 새 문제를 추가하세요.

### 스타일 변경하기

`frontend/src/App.tsx`의 `theme` 객체를 수정하세요.

## 문제 해결

### 포트 3000이 이미 사용 중인 경우

```bash
# vite.config.ts에서 다른 포트로 변경
server: {
  port: 3001,  // 원하는 포트 번호
}
```

### 의존성 설치 오류

```bash
# npm 캐시 클리어 후 재설치
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 빌드 오류

```bash
# TypeScript 타입 체크
npm run build

# 특정 오류가 있다면 해당 파일 확인
```

## 다음 단계

1. **커스텀 문제 만들기**: `sampleProblems.ts`에 자신만의 문제 추가
2. **색상 테마 조정**: 브랜드에 맞는 색상으로 변경
3. **새로운 문제 유형 추가**: 확률, 통계 등 다른 수학 영역 추가
4. **LMS 연동 준비**: 향후 Canvas, Moodle 등과 연동

## 지원

문제가 있거나 도움이 필요하면 GitHub Issues를 통해 문의하세요.
