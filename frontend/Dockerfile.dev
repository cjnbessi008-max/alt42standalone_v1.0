# Frontend Development Dockerfile
FROM node:18-alpine

WORKDIR /app

# 의존성 설치
COPY package*.json ./
RUN npm install

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 5173

# 개발 서버 시작
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
