import React from 'react';
import { Container, Typography } from '@mui/material';

const RoutinePage: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Routine Page</Typography>
    </Container>
  );
};

export default RoutinePage;
