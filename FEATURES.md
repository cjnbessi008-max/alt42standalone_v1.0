# Focus Tracking LMS - Features Documentation

## 🎯 핵심 기능 (Core Features)

### 1. 고난도 집중도 추적 시스템

#### 1.1 다층 집중도 모니터링
- **페이지 포커스/블러 추적**
  - `window.focus` / `window.blur` 이벤트 실시간 감지
  - 학습 창에서 벗어난 시간 자동 계산
  - 멀티태스킹 패턴 분석

- **마우스 활동 분석**
  - 마우스 이동 패턴 (throttled - 1초당 1회)
  - 클릭 위치 및 대상 추적
  - 상호작용 밀도 측정

- **키보드 활동 감지**
  - 입력 이벤트 타이밍 (프라이버시 보호)
  - 키 카테고리 분류 (알파벳, 제어키, 내비게이션 등)
  - 입력 속도 및 패턴 분석

- **스크롤 추적**
  - 문제 읽기 패턴 분석
  - 스크롤 비율 계산
  - 문제 탐색 행동 모니터링

- **비활동(Idle) 감지**
  - 30초 이상 활동 없을 시 자동 감지
  - Idle 시작/종료 이벤트 기록
  - 비활동 시간 별도 집계

#### 1.2 집중도 메트릭 계산

**Attention Score (집중도 점수)**: 0-100점
```javascript
attention_score = (active_time / total_time) × 100
```

**Activity Ratio (활동 비율)**: 0-1
```javascript
activity_ratio = focus_time / (focus_time + idle_time)
```

**Interaction Count (상호작용 횟수)**
- 마우스 클릭, 키보드 입력, 스크롤 이벤트 총합

**Focus Duration (집중 지속 시간)**
- 연속 집중 시간 구간 측정
- 집중 구간 배열 저장

### 2. 안정성 지수 (Stability Index)

#### 2.1 개념
고난도 문제에서도 **일정한 집중도를 유지**하는 능력을 측정하는 지표

#### 2.2 계산 방법

**Step 1: 집중 구간 분산 계산**
```javascript
variance = Σ(focus_period - mean)² / n
```

**Step 2: 안정성 점수 변환**
```javascript
stability_score = min(100, 100 / (1 + variance / 1000))
```

- 분산이 낮을수록 (집중이 일정할수록) → 안정성 점수 ↑
- 분산이 높을수록 (집중이 들쑥날쑥) → 안정성 점수 ↓

#### 2.3 특징
- 단일 집중 구간 = 100점 (완벽한 안정성)
- 집중 구간 없음 = 0점
- 여러 집중 구간의 편차가 작을수록 높은 점수

### 3. 난이도 적응형 점수 시스템

#### 3.1 난이도별 가중치
| 레벨 | 명칭 | 가중치 | 설명 |
|------|------|--------|------|
| 1 | 쉬움 | 1.0x | 기본 점수 |
| 2 | 기본 | 1.2x | 20% 보너스 |
| 3 | 중급 | 1.5x | 50% 보너스 |
| 4 | 고급 | 1.8x | 80% 보너스 |
| 5 | 최고난도 | 2.0x | 100% 보너스 |

#### 3.2 최종 점수 계산식

```javascript
// 가중치 적용
weighted_focus = focus_score × 0.6      // 집중도 60%
weighted_stability = stability_score × 0.2  // 안정성 20%
weighted_accuracy = accuracy_rate × 0.2     // 정확도 20%

// 기본 점수
base_score = weighted_focus + weighted_stability + weighted_accuracy

// 최종 점수 (난이도 가중치 적용, 최대 100점)
final_score = min(100, base_score × difficulty_multiplier)
```

#### 3.3 예시

**시나리오**: 난이도 5 (최고난도) 문제 풀이
- Focus Score: 80점
- Stability Score: 85점
- Accuracy Rate: 90%

**계산:**
```
weighted_focus = 80 × 0.6 = 48
weighted_stability = 85 × 0.2 = 17
weighted_accuracy = 90 × 0.2 = 18

base_score = 48 + 17 + 18 = 83

final_score = min(100, 83 × 2.0) = 100점
```

### 4. 실시간 피드백 시스템

#### 4.1 난이도별 피드백

**고난도 (Level 4-5) 특화 피드백:**
- 안정성 점수 < 70: "고난도 문제에서 집중도가 불안정합니다..."
- 안정성 점수 ≥ 85: "고난도 문제에서도 안정적인 집중 유지!"
- 집중도 < 60: "어려운 문제일수록 집중이 중요합니다..."

**일반 피드백:**
- 완벽 조건 (Focus ≥ 80 & Stability ≥ 80 & Accuracy ≥ 80):
  "완벽합니다! 집중도, 안정성, 정확도 모두 우수합니다."

- 정확도 < 50: "정답률 개선을 위해 문제를 천천히..."

- 집중도 ≥ 85: "높은 집중력을 보여주고 있습니다!"

#### 4.2 성과 등급

| 점수 범위 | 등급 | 라벨 | 색상 |
|-----------|------|------|------|
| 90-100 | excellent | 탁월 | 초록 |
| 80-89 | great | 우수 | 파랑 |
| 70-79 | good | 양호 | 보라 |
| 60-69 | fair | 보통 | 주황 |
| 50-59 | needs_improvement | 노력 필요 | 빨강 |
| 0-49 | poor | 개선 필요 | 진한 빨강 |

### 5. 교사 대시보드

#### 5.1 학급 전체 통계
- 총 학생 수
- 총 학습 세션 수
- 평균 최종 점수
- 평균 집중도 점수
- 평균 정답률

#### 5.2 학생별 분석
**각 학생당 표시 정보:**
- 총 세션 수
- 평균 점수
- 평균 집중도
- 총 학습 시간
- 난이도별 성과 분석

#### 5.3 난이도별 성과 분석
**전체 학급 난이도별:**
- 시도 횟수
- 정답 횟수 및 정답률
- 평균 점수
- 평균 소요 시간
- 평균 집중도

#### 5.4 최근 활동 로그
- 학생명
- 세션 시작 시간
- 학습 시간
- 최종 점수
- 집중도 점수

### 6. 데이터 저장 및 분석

#### 6.1 이벤트 로그
**focus_events 테이블** - 모든 집중도 이벤트 기록
- 이벤트 타입: page_focus, page_blur, mouse_move, mouse_click, keyboard_input, scroll, idle_start, idle_end
- 타임스탬프 (밀리초 정밀도)
- 이벤트 메타데이터 (JSON)
- 세션 및 문제 연결

**파티셔닝:**
- 분기별 파티션 (2025 Q1, Q2, Q3, Q4)
- 효율적인 대용량 데이터 관리

#### 6.2 집중도 메트릭
**focus_metrics 테이블** - 집계된 메트릭
- attention_score (집중도 점수)
- stability_index (안정성 지수)
- activity_ratio (활동 비율)
- idle_time (비활동 시간)
- interaction_count (상호작용 횟수)
- focus_duration (집중 지속 시간)

#### 6.3 세션 점수
**session_scores 테이블** - 최종 점수 및 통계
- 기본 통계: 문제 수, 정답 수, 정답률
- 집중도 점수, 안정성 점수, 활동 점수
- 최종 점수 (가중치 적용)
- 집중/비활동 시간, 집중 비율, 변동성

### 7. 보안 기능

#### 7.1 인증
- JWT (JSON Web Token) 기반 인증
- Bearer 토큰 방식
- 1시간 만료 시간

#### 7.2 SQL Injection 방지
- PDO Prepared Statements 사용
- 모든 쿼리 파라미터화

#### 7.3 XSS 방지
- JSON 응답만 사용
- HTML 이스케이프 처리

#### 7.4 CORS 설정
- Whitelist 기반 Origin 제한
- Preflight 요청 지원

### 8. 성능 최적화

#### 8.1 배치 처리
- 집중도 이벤트 배치 전송 (20개씩)
- 10초마다 자동 제출

#### 8.2 Throttling
- 마우스 이동: 1초당 1회
- 스크롤: 0.5초당 1회
- 과도한 이벤트 방지

#### 8.3 데이터베이스 최적화
- 인덱스: user_id, session_id, event_type, timestamp
- 복합 인덱스: (session_id, event_type, timestamp)
- 파티셔닝: 날짜 기반

#### 8.4 캐싱 전략
- 클라이언트: localStorage (토큰, 사용자 정보)
- 서버: MySQL 쿼리 캐시

### 9. 데이터 프라이버시

#### 9.1 키보드 입력 보호
- 실제 키 값 저장 안 함
- 키 카테고리만 기록 (alphanumeric, control, navigation, delete, other)

#### 9.2 마우스 위치 최소화
- 절대 좌표 저장 (상대 좌표 아님)
- 클릭 대상 태그명만 저장

#### 9.3 개인정보 보호
- 비밀번호 해시 (PASSWORD_DEFAULT)
- 이메일 암호화 가능
- GDPR/PIPA 준수 설계

## 📊 사용 시나리오

### 시나리오 1: 고난도 문제 집중도 유지

**상황:**
- 학생이 난이도 5 문제에 도전
- 10분간 집중하여 풀이

**시스템 동작:**
1. 실시간 집중도 모니터링
2. 페이지 이탈 시 idle 감지
3. 마우스/키보드 활동 추적
4. 안정성 지수 계산 (집중도 변동 최소화)
5. 난이도 가중치 2.0 적용
6. 최종 점수 산출 및 피드백 제공

**결과:**
- 안정적 집중 유지 → 안정성 점수 90+
- 난이도 보너스 → 최종 점수 최대 100점
- 긍정적 피드백 제공

### 시나리오 2: 교사의 학급 모니터링

**상황:**
- 교사가 30명 학생의 학습 현황 확인

**시스템 제공:**
1. 학급 전체 평균 점수/집중도
2. 개별 학생별 상세 통계
3. 난이도별 성과 분석
4. 최근 활동 로그
5. 집중도 패턴 분석

**활용:**
- 집중도 낮은 학생 식별
- 난이도 추천 조정
- 개별 피드백 생성
- 학습 전략 수립

## 🔮 향후 개선 방향

### Phase 2 (Future)
- [ ] Eye-tracking 하드웨어 통합
- [ ] 생체 신호 (심박수, GSR) 추적
- [ ] AI 기반 난이도 자동 조정
- [ ] 예측 학습 분석

### Phase 3 (Integration)
- [ ] Moodle LTI 1.3 연동
- [ ] Canvas, Blackboard 지원
- [ ] SSO (SAML, OAuth) 통합
- [ ] 성적 시스템 동기화

### Advanced Features
- [ ] 멀티플레이어 학습 모드
- [ ] 게이미피케이션 요소
- [ ] 음성 입력 지원
- [ ] 모바일 네이티브 앱
- [ ] 오프라인 모드
